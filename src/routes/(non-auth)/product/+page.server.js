export const load = async () => {

	return {};
};

export const actions = {
	// default: async ({ fetch, cookies }) => {
	// 	//TODO server-side form validation is necessary


	// 	//return errors to client side
	// 	//alert or something else
	// }
};
