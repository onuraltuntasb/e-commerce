export const load = async ({ url }) => {

    console.log("param : ", url.searchParams.get('category'));


    return {};
};

export const actions = {
    // default: async ({ fetch, cookies }) => {

    // }
};
